@page "/Account/ForgotPasswordConfirmation"

@inject NavigationManager NavigationManager

<PageTitle>Correo enviado</PageTitle>

<div class="auth-page">
    <div class="auth-card fade-in">
        <div class="auth-icon">📧</div>

        <h1 class="auth-title">Revisa tu correo</h1>
        <p class="auth-subtitle">
            Si el correo existe en nuestro sistema, hemos enviado un enlace para restablecer tu contraseña.
        </p>

        <p class="small text-muted mb-4">
            Abre tu bandeja de entrada y sigue las instrucciones del mensaje.
            Si no lo ves, revisa también la carpeta de correo no deseado.
        </p>

        <button type="button"
                class="btn btn-primary auth-submit w-100"
                @onclick="IrALogin">
            Ir a iniciar sesión
        </button>
    </div>

    <div class="auth-links fade-in-delayed">
        <span class="small">
            ¿Ya restableciste tu contraseña?
            <a href="/Account/Login" class="link-light">Inicia sesión aquí</a>
        </span>
    </div>
</div>

@code {
    private void IrALogin()
    {
        NavigationManager.NavigateTo("/Account/Login");
    }
}
